@page "/approvals/details/{RequestId:int}"
@layout Components.Layout.MainLayout

@using Microsoft.AspNetCore.Components.Forms
@using ChurchApp.Components.Pages.Approvals.Forms
@using ChurchApp.Models
@using ChurchApp.Services

@inject LeaveRequestService LeaveService
@inject ApprovalRequestService ApprovalRequestService
@inject AuthService AuthService
@inject NavigationManager Navigation
@inject HtmlCleanService HtmlCleanService

<PageTitle>Approval Request Details - BCC ServiceHub</PageTitle>

@if (AuthService.IsAuthenticated)
{
    <div class="worker360-page approval-details-page">

        @if (!string.IsNullOrWhiteSpace(errorMessage))
        {
            <div class="alert alert-danger compact-alert">
                <i class="fas fa-exclamation-circle me-2"></i>
                @errorMessage
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(successMessage))
        {
            <div class="alert alert-success compact-alert">
                <i class="fas fa-check-circle me-2"></i>
                @successMessage
            </div>
        }

        @if (request != null)
        {
            <div class="bcc-page-header">
                <div>
                    <div class="bcc-page-subtitle">
                        <i class="fas fa-clipboard-list me-2"></i>
                        Approval Hub
                    </div>

                    <div class="bcc-page-title">@request.Subject</div>

                    <div class="bcc-page-subtitle">
                        @request.RequestCode • @request.RequestType?.Name • @request.Status
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="button"
                            class="bcc-btn profile-form-secondary-btn"
                            @onclick="GoBack">
                        <i class="fas fa-arrow-left me-2"></i>Back
                    </button>
                </div>
            </div>

            <div class="approval-tab-card">

                <div class="approval-tabs">
                    <button type="button"
                            class="@GetCompactTabClass("details")"
                            @onclick='() => SetTab("details")'>
                        <i class="fas fa-file-lines"></i>
                        Request Details
                    </button>

                    <button type="button"
                            class="@GetCompactTabClass("timeline")"
                            @onclick='() => SetTab("timeline")'>
                        <i class="fas fa-clock-rotate-left"></i>
                        Timeline
                    </button>
                </div>

                @if (activeTab == "details")
                {
                    <div class="approval-two-column">

                        <div class="approval-column">

                            <section class="compact-section">
                                <div class="compact-section-header">
                                    <div>
                                        <i class="fas fa-circle-info"></i>
                                        <span>Request Information</span>
                                    </div>
                                </div>

                                <div class="compact-info-grid">
                                    @CompactInfo("Request Code", request.RequestCode)
                                    @CompactInfo("Request Type", request.RequestType?.Name ?? "-")
                                    @CompactInfo("Requested By", $"{request.RequestedByWorker?.FirstName} {request.RequestedByWorker?.LastName}")
                                    @CompactInfo("Status", request.Status)
                                    @CompactInfo("Directorate", request.Directorate?.Name ?? "Not assigned")
                                    @CompactInfo("Department", request.Department?.Name ?? "Not assigned")
                                    @CompactInfo("Submitted", request.SubmittedAt?.ToString("dd MMM yyyy, HH:mm") ?? "-")
                                    @CompactInfo("Current Step", request.CurrentStepOrder.ToString())
                                    @CompactInfo("Pending With", GetPendingWithText())
                                    @CompactInfoMarkup("Final Approved By", GetFinalApprovedByMarkup())
                                </div>
                            </section>

                            @if (financialDetails != null)
                            {
                                <section class="compact-section">
                                    <div class="compact-section-header financial">
                                        <div>
                                            <i class="fas fa-coins"></i>
                                            <span>Financial Information</span>
                                        </div>
                                    </div>

                                    <div class="compact-info-grid">
                                        @CompactInfo("Amount Requested", $"₦{financialDetails.AmountRequested:N2}")
                                        @CompactInfoMarkup("Amount Approved", GetAmountApprovedMarkup())
                                        @CompactInfo("Budget Line", financialDetails.BudgetLine ?? "Not provided")
                                        @CompactInfo("Purpose", financialDetails.Purpose ?? "Not provided")
                                        @CompactInfo("Payment Details", financialDetails.PaymentDetails ?? "Not provided")
                                    </div>
                                </section>
                            }

                            @if (IsLeaveRequest && leaveDetails != null)
                            {
                                <section class="compact-section request-specific-section">
                                    <LeaveRequestDetails Details="@leaveDetails" />
                                </section>
                            }

                            @if (!IsLeaveRequest)
                            {
                                <section class="compact-section">
                                    <div class="compact-section-header">
                                        <div>
                                            <i class="fas fa-align-left"></i>
                                            <span>Details / Background</span>
                                        </div>
                                    </div>

                                    <div class="compact-rich-box approval-rich-content">
                                        @((MarkupString)HtmlCleanService.Clean(request.Details))
                                    </div>
                                </section>

                                <section class="compact-section">
                                    <div class="compact-section-header approval-sought">
                                        <div>
                                            <i class="fas fa-circle-check"></i>
                                            <span>Approval Sought</span>
                                        </div>
                                    </div>

                                    <div class="compact-rich-box approval-rich-content">
                                        @((MarkupString)HtmlCleanService.Clean(request.ApprovalSought))
                                    </div>
                                </section>
                            }

                        </div>

                        <div class="approval-column">

                            <section class="compact-section">
                                <div class="compact-section-header workflow">
                                    <div>
                                        <i class="fas fa-route"></i>
                                        <span>Workflow Progress</span>
                                    </div>
                                </div>

                                @if (workflowSteps.Any())
                                {
                                    <div class="compact-workflow">

                                        <div class="compact-workflow-row completed">
                                            <span class="workflow-dot">
                                                <i class="fas fa-check"></i>
                                            </span>

                                            <div>
                                                <strong>Submitted</strong>
                                                <small>Request successfully submitted</small>
                                            </div>
                                        </div>

                                        @foreach (var step in workflowSteps.OrderBy(x => x.StepOrder))
                                        {
                                            var progressStatus =
                                                GetWorkflowStepStatus(step);

                                            <div class="compact-workflow-row @progressStatus">
                                                <span class="workflow-dot">
                                                    <i class="@GetWorkflowStepIcon(progressStatus)"></i>
                                                </span>

                                                <div>
                                                    <strong>@step.StepName</strong>
                                                    <small>
                                                        @GetWorkflowStepDescription(step, progressStatus)
                                                    </small>
                                                </div>
                                            </div>
                                        }

                                    </div>
                                }
                                else
                                {
                                    <div class="compact-empty">
                                        No workflow information is available.
                                    </div>
                                }
                            </section>

                            <section class="compact-section">
                                <div class="compact-section-header attachments">
                                    <div>
                                        <i class="fas fa-paperclip"></i>
                                        <span>Supporting Documents</span>
                                    </div>

                                    <span class="section-count">
                                        @attachments.Count
                                    </span>
                                </div>

                                @if (attachments.Any())
                                {
                                    <div class="compact-attachment-list">
                                        @foreach (var attachment in attachments)
                                        {
                                            <div class="compact-attachment">
                                                <span class="attachment-icon">
                                                    <i class="@GetAttachmentIcon(attachment.FileName)"></i>
                                                </span>

                                                <div class="attachment-details">
                                                    <strong>@attachment.FileName</strong>
                                                    <small>
                                                        @FormatAttachmentSize(attachment.FileSize)
                                                        • @attachment.UploadedAt.ToString("dd MMM yyyy")
                                                    </small>
                                                </div>

                                                <a href="/@attachment.FilePath"
                                                   target="_blank"
                                                   rel="noopener noreferrer"
                                                   class="compact-icon-btn"
                                                   title="View attachment">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                            </div>
                                        }
                                    </div>
                                }
                                else
                                {
                                    <div class="compact-empty">
                                        No supporting document was attached.
                                    </div>
                                }
                            </section>

                            @if (canActOnRequest &&
                                 request.Status != "Approved" &&
                                 request.Status != "Rejected")
                            {
                                <section class="compact-section action-section">
                                    <div class="compact-section-header action">
                                        <div>
                                            <i class="fas fa-gavel"></i>
                                            <span>Approval Action</span>
                                        </div>
                                    </div>

                                    <div class="compact-action-body">

                                        @if (IsFinalFinancialApproval)
                                        {
                                            <div class="compact-financial-box">
                                                <label class="form-label">
                                                    Amount Approved
                                                    <span class="text-danger">*</span>
                                                </label>

                                                <div class="input-group input-group-sm">
                                                    <span class="input-group-text">₦</span>

                                                    <InputNumber class="form-control"
                                                                 @bind-Value="amountApproved" />
                                                </div>

                                                <small>
                                                    Requested:
                                                    ₦@(financialDetails?.AmountRequested.ToString("N2") ?? "0.00")
                                                </small>
                                            </div>
                                        }

                                        <label class="form-label">
                                            Decision Comment
                                            <span class="text-danger">*</span>
                                        </label>

                                        <textarea class="form-control form-control-sm"
                                                  rows="3"
                                                  @bind="decisionComment"
                                                  placeholder="Enter a brief comment for this action."></textarea>

                                        <div class="compact-action-buttons">
                                            <button type="button"
                                                    class="bcc-btn bcc-btn-primary compact-btn"
                                                    disabled="@isProcessing"
                                                    @onclick="ApproveRequest">
                                                <i class="fas fa-check me-1"></i>
                                                Approve
                                            </button>

                                            <button type="button"
                                                    class="compact-danger-btn"
                                                    disabled="@isProcessing"
                                                    @onclick="RejectRequest">
                                                <i class="fas fa-times me-1"></i>
                                                Reject
                                            </button>

                                            <button type="button"
                                                    class="compact-moreinfo-btn"
                                                    disabled="@isProcessing"
                                                    @onclick="RequestMoreInfo">
                                                <i class="fas fa-question-circle me-1"></i>
                                                More Info
                                            </button>
                                        </div>

                                    </div>
                                </section>
                            }

                            @if (canResubmit)
                            {
                                <section class="compact-section">
                                    <div class="compact-section-header returned">
                                        <div>
                                            <i class="fas fa-reply"></i>
                                            <span>Respond and Resubmit</span>
                                        </div>
                                    </div>

                                    <div class="compact-action-body">
                                        <label class="form-label">
                                            Response / Clarification
                                            <span class="text-danger">*</span>
                                        </label>

                                        <textarea class="form-control form-control-sm mb-2"
                                                  rows="3"
                                                  @bind="resubmitComment"
                                                  placeholder="Provide the requested clarification."></textarea>

                                        <label class="form-label">
                                            Updated Details
                                        </label>

                                        <textarea class="form-control form-control-sm mb-2"
                                                  rows="3"
                                                  @bind="resubmitDetails"></textarea>

                                        <label class="form-label">
                                            Updated Approval Sought
                                        </label>

                                        <textarea class="form-control form-control-sm"
                                                  rows="2"
                                                  @bind="resubmitApprovalSought"></textarea>

                                        <div class="mt-2">
                                            <button type="button"
                                                    class="bcc-btn bcc-btn-primary compact-btn"
                                                    disabled="@isProcessing"
                                                    @onclick="ResubmitRequest">
                                                <i class="fas fa-paper-plane me-1"></i>
                                                Resubmit
                                            </button>
                                        </div>
                                    </div>
                                </section>
                            }

                        </div>

                    </div>
                }

                @if (activeTab == "timeline")
                {
                    <div class="timeline-panel">
                        <div class="compact-section-header">
                            <div>
                                <i class="fas fa-clock-rotate-left"></i>
                                <span>Request Timeline</span>
                            </div>
                        </div>

                        @if (actions.Any())
                        {
                            <div class="compact-timeline">

                                @foreach (var action in actions.OrderByDescending(x => x.CreatedAt))
                                {
                                    <div class="compact-timeline-item">
                                        <span class="compact-timeline-dot @GetTimelineDotClass(action.ActionType)">
                                            <i class="@GetTimelineIcon(action.ActionType)"></i>
                                        </span>

                                        <div class="compact-timeline-card">
                                            <div class="compact-timeline-top">
                                                <div>
                                                    <strong>
                                                        @GetTimelineActionName(action.ActionType)
                                                    </strong>

                                                    <span class="@GetTimelineBadgeClass(action.ActionType)">
                                                        @GetTimelineActionName(action.ActionType)
                                                    </span>
                                                </div>

                                                <small>
                                                    @action.CreatedAt.ToString("dd MMM yyyy, HH:mm")
                                                </small>
                                            </div>

                                            <div class="compact-timeline-user">
                                                <i class="fas fa-user-circle"></i>
                                                @action.ActionByWorker?.FirstName
                                                @action.ActionByWorker?.LastName
                                            </div>

                                            @if (!string.IsNullOrWhiteSpace(action.Comment))
                                            {
                                                <div class="compact-timeline-comment">
                                                    @action.Comment
                                                </div>
                                            }

                                            @if (action.FromStepOrder.HasValue ||
                                                 action.ToStepOrder.HasValue)
                                            {
                                                <div class="compact-timeline-movement">
                                                    Level @(action.FromStepOrder ?? 0)
                                                    <i class="fas fa-arrow-right mx-1"></i>
                                                    Level @(action.ToStepOrder ?? 0)
                                                </div>
                                            }
                                        </div>
                                    </div>
                                }

                            </div>
                        }
                        else
                        {
                            <div class="compact-empty py-4">
                                No action has been recorded for this request.
                            </div>
                        }
                    </div>
                }

            </div>
        }
        else
        {
            <div class="bcc-empty">
                <i class="fas fa-file-circle-xmark"></i>
                <h4>Request Not Found</h4>
                <p>The approval request could not be found.</p>

                <button type="button"
                        class="bcc-btn bcc-btn-primary compact-btn"
                        @onclick="GoBack">
                    Back
                </button>
            </div>
        }
    </div>
}
else
{
    <div class="bcc-empty">
        <i class="fas fa-lock"></i>
        <h4>Please Log In</h4>
        <p>You need to be logged in to view approval requests.</p>
        <a href="/login" class="bcc-btn bcc-btn-primary">Go to Login</a>
    </div>
}

@if (showSuccessPopup)
{
    <div class="approval-popup-backdrop">
        <div class="approval-popup-card">
            <div class="approval-popup-icon">
                <i class="fas fa-check-circle"></i>
            </div>

            <h4>Action Successful</h4>
            <p>@popupMessage</p>

            <button type="button"
                    class="bcc-btn bcc-btn-primary compact-btn"
                    @onclick="CloseSuccessPopup">
                OK
            </button>
        </div>
    </div>
}


@code {
    [Parameter]
    public int RequestId { get; set; }

    private ApprovalRequest? request;
    private ApprovalDecision? finalApprovalDecision;
    private FinancialRequestDetail? financialDetails;
    private ApprovalWorkflowStep? currentWorkflowStep;

    private List<ApprovalRequestAction> actions = new();
    private List<ApprovalWorkflowStep> workflowSteps = new();
    private List<ApprovalRequestAttachment> attachments = new();

    private string activeTab = "details";
    private string? errorMessage;
    private string? successMessage;
    private string decisionComment = string.Empty;
    private string resubmitComment = string.Empty;
    private string resubmitDetails = string.Empty;
    private string resubmitApprovalSought = string.Empty;
    private string popupMessage = string.Empty;

    private bool canActOnRequest;
    private bool canResubmit;
    private bool showSuccessPopup;
    private bool isProcessing;

    private decimal? amountApproved;
    private LeaveRequestDetail? leaveDetails;

private bool IsLeaveRequest =>
    string.Equals(
        request?.RequestType?.Code,
        "Leave-Request",
        StringComparison.OrdinalIgnoreCase);

    private bool IsFinancialRequest =>
        string.Equals(
            request?.RequestType?.Code,
            "Financial-Request",
            StringComparison.OrdinalIgnoreCase);

    private bool IsFinalFinancialApproval =>
        IsFinancialRequest &&
        currentWorkflowStep?.IsFinalStep == true &&
        canActOnRequest;

    protected override async Task OnParametersSetAsync()
    {
        await LoadRequest();
    }

    private async Task LoadRequest()
{
    try
    {
        errorMessage = null;

        request = await ApprovalRequestService
            .GetRequestByIdAsync(RequestId);

        actions = await ApprovalRequestService
            .GetRequestActionsAsync(RequestId);

        finalApprovalDecision = await ApprovalRequestService
            .GetFinalApprovalDecisionAsync(RequestId);

        attachments = await ApprovalRequestService
            .GetRequestAttachmentsAsync(RequestId);

        workflowSteps.Clear();

        currentWorkflowStep = null;
        financialDetails = null;
        leaveDetails = null;

        amountApproved = null;
        canActOnRequest = false;
        canResubmit = false;

        if (request != null)
        {
            workflowSteps = await ApprovalRequestService
                .GetWorkflowStepsAsync(
                    request.WorkflowDefinitionId);

            currentWorkflowStep = workflowSteps
                .FirstOrDefault(x =>
                    x.StepOrder == request.CurrentStepOrder);

            /*
             * Financial Request-specific information
             */
            if (IsFinancialRequest)
            {
                financialDetails = await ApprovalRequestService
                    .GetFinancialRequestDetailsAsync(RequestId);

                amountApproved =
                    financialDetails?.AmountApproved
                    ?? financialDetails?.AmountRequested;
            }

            /*
             * Leave Request-specific information
             */
            if (IsLeaveRequest)
            {
                leaveDetails = await LeaveService
    .GetLeaveRequestDetailsAsync(RequestId);
            }
        }

        if (request != null &&
            AuthService.CurrentWorker != null)
        {
            canActOnRequest = await ApprovalRequestService
                .CanWorkerActOnRequestAsync(
                    request.Id,
                    AuthService.CurrentWorker.Id);

            canResubmit =
                request.RequestedByWorkerId ==
                    AuthService.CurrentWorker.Id &&
                request.Status == "MoreInfoRequested";

            if (canResubmit)
            {
                resubmitDetails = request.Details;
                resubmitApprovalSought =
                    request.ApprovalSought;
            }
        }
    }
    catch (Exception ex)
    {
        errorMessage =
            $"Error loading request: {ex.Message}";
    }
}

    private void SetTab(string tab)
    {
        activeTab = tab;
    }

    private string GetTabClass(string tab)
    {
        return activeTab == tab
            ? "profile-form-tab active"
            : "profile-form-tab";
    }

    private void GoBack()
    {
        Navigation.NavigateTo("/approvals/my-requests");
    }

    private void CloseSuccessPopup()
    {
        showSuccessPopup = false;
        Navigation.NavigateTo("/approvals/inbox");
    }

    private async Task ApproveRequest()
    {
        await ProcessAction("Approve");
    }

    private async Task RejectRequest()
    {
        await ProcessAction("Reject");
    }

    private async Task RequestMoreInfo()
    {
        await ProcessAction("MoreInfo");
    }

    private async Task ProcessAction(string action)
    {
        if (isProcessing)
            return;

        errorMessage = null;
        successMessage = null;

        if (AuthService.CurrentWorker == null)
        {
            errorMessage = "Unable to identify current worker.";
            return;
        }

        if (string.IsNullOrWhiteSpace(decisionComment))
        {
            errorMessage = "Comment is required before taking action.";
            return;
        }

        if (action == "Approve" && IsFinalFinancialApproval)
        {
            if (!amountApproved.HasValue || amountApproved.Value <= 0)
            {
                errorMessage = "Please enter a valid approved amount.";
                return;
            }

            if (financialDetails != null &&
                amountApproved.Value > financialDetails.AmountRequested)
            {
                errorMessage =
                    "Amount approved cannot be greater than the amount requested.";
                return;
            }
        }

        try
        {
            isProcessing = true;

            if (action == "Approve")
            {
                await ApprovalRequestService.ApproveRequestAsync(
                    RequestId,
                    AuthService.CurrentWorker.Id,
                    decisionComment,
                    IsFinalFinancialApproval ? amountApproved : null);

                popupMessage = "Request approved successfully.";
            }
            else if (action == "Reject")
            {
                await ApprovalRequestService.RejectRequestAsync(
                    RequestId,
                    AuthService.CurrentWorker.Id,
                    decisionComment);

                popupMessage = "Request rejected successfully.";
            }
            else if (action == "MoreInfo")
            {
                await ApprovalRequestService.RequestMoreInfoAsync(
                    RequestId,
                    AuthService.CurrentWorker.Id,
                    decisionComment);

                popupMessage = "More information requested successfully.";
            }

            decisionComment = string.Empty;
            showSuccessPopup = true;
        }
        catch (Exception ex)
        {
            errorMessage = $"Error processing action: {ex.Message}";
        }
        finally
        {
            isProcessing = false;
        }
    }

    private async Task ResubmitRequest()
    {
        if (isProcessing)
            return;

        errorMessage = null;
        successMessage = null;

        if (AuthService.CurrentWorker == null)
        {
            errorMessage = "Unable to identify current worker.";
            return;
        }

        if (string.IsNullOrWhiteSpace(resubmitComment))
        {
            errorMessage =
                "Response / clarification is required before resubmitting.";
            return;
        }

        try
        {
            isProcessing = true;

            await ApprovalRequestService.ResubmitRequestAsync(
                RequestId,
                AuthService.CurrentWorker.Id,
                resubmitComment,
                resubmitDetails,
                resubmitApprovalSought);

            popupMessage = "Request resubmitted successfully.";
            showSuccessPopup = true;
        }
        catch (Exception ex)
        {
            errorMessage = $"Error resubmitting request: {ex.Message}";
        }
        finally
        {
            isProcessing = false;
        }
    }

    private string GetPendingWithText()
    {
        if (request == null)
            return "-";

        if (request.Status == "Approved")
            return "Completed";

        if (request.Status == "Rejected")
            return "Rejected";

        if (request.Status == "MoreInfoRequested")
            return "Returned to Initiator";

        if (request.CurrentApproverWorker != null)
        {
            return $"{request.CurrentApproverWorker.FirstName} " +
                   $"{request.CurrentApproverWorker.LastName}";
        }

        return FormatApproverType(request.CurrentApproverType ?? "");
    }

    private MarkupString GetFinalApprovedByMarkup()
    {
        if (request?.Status != "Approved")
        {
            return new MarkupString(
                "<span class='text-danger fw-bold'>Not yet approved</span>");
        }

        if (finalApprovalDecision?.DecisionByWorker != null)
        {
            var name =
                $"{finalApprovalDecision.DecisionByWorker.FirstName} " +
                $"{finalApprovalDecision.DecisionByWorker.LastName}";

            return new MarkupString(
                $"<span class='text-success fw-bold'>{System.Net.WebUtility.HtmlEncode(name)}</span>");
        }

        return new MarkupString(
            "<span class='text-success fw-bold'>Approved</span>");
    }

    private MarkupString GetAmountApprovedMarkup()
    {
        if (financialDetails?.AmountApproved.HasValue == true)
        {
            return new MarkupString(
                $"<span class='text-success fw-bold'>₦{financialDetails.AmountApproved.Value:N2}</span>");
        }

        return new MarkupString(
            "<span class='text-danger fw-bold'>Not yet approved</span>");
    }

    private string GetWorkflowStepStatus(ApprovalWorkflowStep step)
    {
        if (request == null)
            return "upcoming";

        if (request.Status == "Rejected" &&
            request.CurrentStepOrder == step.StepOrder)
        {
            return "rejected";
        }

        if (request.Status == "MoreInfoRequested" &&
            request.CurrentStepOrder == step.StepOrder)
        {
            return "returned";
        }

        if (request.Status == "Approved")
            return "completed";

        if (step.StepOrder < request.CurrentStepOrder)
            return "completed";

        if (step.StepOrder == request.CurrentStepOrder)
            return "current";

        return "upcoming";
    }

    private string GetWorkflowStepIcon(string status)
    {
        return status switch
        {
            "completed" => "fas fa-check",
            "current" => "fas fa-hourglass-half",
            "rejected" => "fas fa-times",
            "returned" => "fas fa-reply",
            _ => "fas fa-circle"
        };
    }

    private string GetWorkflowStepDescription(
        ApprovalWorkflowStep step,
        string status)
    {
        return status switch
        {
            "completed" => "Completed",
            "current" => GetCurrentStepDescription(step),
            "rejected" => "Request rejected at this level",
            "returned" => "More information requested",
            _ => "Pending"
        };
    }

    private string GetCurrentStepDescription(ApprovalWorkflowStep step)
    {
        if (request?.CurrentApproverWorker != null)
        {
            return $"Pending with {request.CurrentApproverWorker.FirstName} " +
                   $"{request.CurrentApproverWorker.LastName}";
        }

        return $"Pending with {FormatApproverType(step.ApproverType)}";
    }

    private string FormatApproverType(string approverType)
    {
        return approverType switch
        {
            "HeadOfDirectorate" => "Head of Directorate",
            "HeadOfService" => "Head of Service",
            "Pastor" => "Pastor",
            "ChurchAdmin" => "Church Admin",
            "SpecificWorker" => "Assigned Worker",
            "PrivilegeBased" => "Authorized Reviewer",
            _ => string.IsNullOrWhiteSpace(approverType)
                ? "Not assigned"
                : approverType
        };
    }

    private string GetTimelineActionName(string actionType)
    {
        return actionType switch
        {
            "MoreInfoRequested" => "More Information Requested",
            "Resubmitted" => "Request Resubmitted",
            "Submitted" => "Request Submitted",
            "Approved" => "Request Approved",
            "Rejected" => "Request Rejected",
            "Forwarded" => "Request Forwarded",
            "Commented" => "Comment Added",
            _ => actionType
        };
    }

    private string GetTimelineIcon(string actionType)
    {
        return actionType switch
        {
            "Submitted" => "fas fa-paper-plane",
            "Approved" => "fas fa-check",
            "Rejected" => "fas fa-times",
            "MoreInfoRequested" => "fas fa-question",
            "Resubmitted" => "fas fa-reply",
            "Forwarded" => "fas fa-share",
            _ => "fas fa-circle"
        };
    }

    private string GetTimelineDotClass(string actionType)
    {
        return actionType switch
        {
            "Approved" => "timeline-success",
            "Rejected" => "timeline-danger",
            "MoreInfoRequested" => "timeline-warning",
            "Resubmitted" => "timeline-primary",
            "Submitted" => "timeline-primary",
            _ => "timeline-secondary"
        };
    }

    private string GetTimelineBadgeClass(string actionType)
    {
        return actionType switch
        {
            "Approved" => "badge bg-success",
            "Rejected" => "badge bg-danger",
            "MoreInfoRequested" => "badge bg-warning text-dark",
            "Resubmitted" => "badge bg-primary",
            "Submitted" => "badge bg-info text-dark",
            _ => "badge bg-secondary"
        };
    }

    private string GetAttachmentIcon(string fileName)
    {
        return Path.GetExtension(fileName).ToLowerInvariant() switch
        {
            ".pdf" => "fas fa-file-pdf text-danger",
            ".doc" or ".docx" => "fas fa-file-word text-primary",
            ".xls" or ".xlsx" => "fas fa-file-excel text-success",
            ".jpg" or ".jpeg" or ".png" => "fas fa-file-image text-warning",
            _ => "fas fa-file text-secondary"
        };
    }

    private string FormatAttachmentSize(long bytes)
    {
        if (bytes >= 1024 * 1024)
            return $"{bytes / (1024d * 1024d):0.##} MB";

        if (bytes >= 1024)
            return $"{bytes / 1024d:0.##} KB";

        return $"{bytes} bytes";
    }

    private RenderFragment InfoRow(string label, string value) => @<div class="bcc-info-row">
        <div class="bcc-info-label">@label</div>
        <div class="bcc-info-value">@value</div>
    </div>;

    private RenderFragment InfoRowMarkup(string label, MarkupString value) => @<div class="bcc-info-row">
        <div class="bcc-info-label">@label</div>
        <div class="bcc-info-value">@value</div>
    </div>;

    private string GetCompactTabClass(string tab)
    {
        return activeTab == tab
            ? "approval-tab active"
            : "approval-tab";
    }

    private string GetStatusBadgeClass(string status)
    {
        return status switch
        {
            "Approved" => "approval-status-badge approved",
            "Rejected" => "approval-status-badge rejected",
            "MoreInfoRequested" => "approval-status-badge returned",
            "Pending" => "approval-status-badge pending",
            _ => "approval-status-badge neutral"
        };
    }

    private RenderFragment CompactInfo(string label, string value) => @<div class="compact-info-item">
        <span>@label</span>
        <strong>@value</strong>
    </div>;

    private RenderFragment CompactInfoMarkup(string label, MarkupString value) => @<div class="compact-info-item">
        <span>@label</span>
        <strong>@value</strong>
    </div>;
}


<style>
    .approval-details-page {
        max-width: 1480px;
        margin: 0 auto;
    }

    .compact-alert {
        margin-bottom: 10px;
        padding: 8px 10px;
        font-size: 0.72rem;
    }

    .approval-page-topbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 14px;
        margin-bottom: 10px;
        padding: 11px 13px;
        background: #ffffff;
        border: 1px solid #e5e7eb;
        border-left: 4px solid #4f46e5;
        border-radius: 12px;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.035);
    }

    .approval-page-heading,
    .approval-title {
        display: flex;
        align-items: center;
        gap: 10px;
        min-width: 0;
    }

    .approval-heading-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 38px;
        width: 38px;
        height: 38px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 10px;
        font-size: 0.8rem;
    }

    .approval-eyebrow {
        margin-bottom: 1px;
        color: #6366f1;
        font-size: 0.57rem;
        font-weight: 800;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .approval-page-heading h3 {
        max-width: 800px;
        margin: 0;
        overflow: hidden;
        color: #0f172a;
        font-size: 0.96rem;
        font-weight: 800;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .approval-heading-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 4px 10px;
        margin-top: 2px;
        color: #64748b;
        font-size: 0.59rem;
    }

    .approval-topbar-actions {
        display: flex;
        align-items: center;
        gap: 7px;
        flex-shrink: 0;
    }

    .compact-btn {
        min-height: 30px;
        padding: 5px 10px;
        font-size: 0.68rem;
    }

    .approval-status-badge {
        display: inline-flex;
        align-items: center;
        padding: 5px 9px;
        border-radius: 999px;
        font-size: 0.6rem;
        font-weight: 800;
        white-space: nowrap;
    }

    .approval-status-badge.approved {
        color: #166534;
        background: #dcfce7;
    }

    .approval-status-badge.rejected {
        color: #991b1b;
        background: #fee2e2;
    }

    .approval-status-badge.pending {
        color: #92400e;
        background: #fef3c7;
    }

    .approval-status-badge.returned {
        color: #9a3412;
        background: #ffedd5;
    }

    .approval-status-badge.neutral {
        color: #475569;
        background: #e2e8f0;
    }

    .approval-tab-card {
        overflow: hidden;
        background: #f8fafc;
        border: 1px solid #e5e7eb;
        border-radius: 12px;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.035);
    }

    .approval-tabs {
        display: flex;
        gap: 4px;
        padding: 7px 8px 0;
        background: #ffffff;
        border-bottom: 1px solid #e5e7eb;
    }

    .approval-tab {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 7px 10px;
        color: #64748b;
        background: transparent;
        border: 0;
        border-bottom: 2px solid transparent;
        font-size: 0.66rem;
        font-weight: 700;
    }

    .approval-tab.active {
        color: #4338ca;
        border-bottom-color: #4f46e5;
    }

    .approval-two-column {
        display: grid;
        grid-template-columns:
            minmax(0, 1.08fr) minmax(0, 0.92fr);
        gap: 10px;
        padding: 10px;
        align-items: start;
    }

    .approval-column {
        display: flex;
        flex-direction: column;
        gap: 10px;
        min-width: 0;
    }

    .compact-section {
        overflow: hidden;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 10px;
    }

    .compact-section-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        min-height: 34px;
        padding: 7px 9px;
        color: #4338ca;
        background: #eef2ff;
        border-bottom: 1px solid #c7d2fe;
    }

    .compact-section-header > div {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .compact-section-header span {
        color: #0f172a;
        font-size: 0.67rem;
        font-weight: 800;
    }

    .compact-section-header i {
        font-size: 0.64rem;
    }

    .compact-section-header.financial {
        color: #166534;
        background: #f0fdf4;
        border-bottom-color: #bbf7d0;
    }

    .compact-section-header.workflow {
        color: #6d28d9;
        background: #f5f3ff;
        border-bottom-color: #ddd6fe;
    }

    .compact-section-header.attachments {
        color: #0369a1;
        background: #f0f9ff;
        border-bottom-color: #bae6fd;
    }

    .compact-section-header.action {
        color: #92400e;
        background: #fffbeb;
        border-bottom-color: #fde68a;
    }

    .compact-section-header.returned {
        color: #9a3412;
        background: #fff7ed;
        border-bottom-color: #fed7aa;
    }

    .compact-section-header.approval-sought {
        color: #166534;
        background: #f0fdf4;
        border-bottom-color: #bbf7d0;
    }

    .section-count {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 22px;
        height: 22px;
        padding: 0 6px;
        color: #ffffff !important;
        background: #0284c7;
        border-radius: 999px;
        font-size: 0.56rem !important;
    }

    .compact-info-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0;
        padding: 4px 9px;
    }

    .compact-info-item {
        min-width: 0;
        padding: 6px 6px;
        border-bottom: 1px solid #f1f5f9;
    }

    .compact-info-item span,
    .compact-info-item strong {
        display: block;
    }

    .compact-info-item span {
        margin-bottom: 1px;
        color: #64748b;
        font-size: 0.55rem;
        font-weight: 700;
        text-transform: uppercase;
    }

    .compact-info-item strong {
        overflow-wrap: anywhere;
        color: #0f172a;
        font-size: 0.66rem;
        font-weight: 700;
    }

    .compact-rich-box {
        min-height: 46px;
        padding: 9px 10px;
        color: #334155;
        font-size: 0.67rem;
        line-height: 1.45;
        overflow-wrap: anywhere;
    }

    .compact-rich-box p:last-child {
        margin-bottom: 0;
    }

    .compact-workflow {
        padding: 7px 9px;
    }

    .compact-workflow-row {
        position: relative;
        display: flex;
        align-items: flex-start;
        gap: 8px;
        min-height: 38px;
        padding-bottom: 7px;
    }

    .compact-workflow-row:not(:last-child)::after {
        content: "";
        position: absolute;
        top: 24px;
        bottom: -2px;
        left: 10px;
        width: 2px;
        background: #e2e8f0;
    }

    .workflow-dot {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 22px;
        width: 22px;
        height: 22px;
        color: #64748b;
        background: #e2e8f0;
        border-radius: 50%;
        font-size: 0.52rem;
    }

    .compact-workflow-row.completed .workflow-dot {
        color: #ffffff;
        background: #16a34a;
    }

    .compact-workflow-row.current .workflow-dot {
        color: #ffffff;
        background: #f59e0b;
    }

    .compact-workflow-row.rejected .workflow-dot {
        color: #ffffff;
        background: #dc2626;
    }

    .compact-workflow-row.returned .workflow-dot {
        color: #ffffff;
        background: #f97316;
    }

    .compact-workflow-row strong,
    .compact-workflow-row small {
        display: block;
    }

    .compact-workflow-row strong {
        color: #0f172a;
        font-size: 0.64rem;
    }

    .compact-workflow-row small {
        margin-top: 1px;
        color: #64748b;
        font-size: 0.56rem;
        line-height: 1.35;
    }

    .compact-attachment-list {
        padding: 5px 8px;
    }

    .compact-attachment {
        display: flex;
        align-items: center;
        gap: 7px;
        padding: 6px 2px;
        border-bottom: 1px solid #f1f5f9;
    }

    .attachment-icon {
        flex: 0 0 24px;
        font-size: 0.8rem;
        text-align: center;
    }

    .attachment-details {
        min-width: 0;
        flex: 1;
    }

    .attachment-details strong,
    .attachment-details small {
        display: block;
    }

    .attachment-details strong {
        overflow: hidden;
        color: #0f172a;
        font-size: 0.62rem;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .attachment-details small {
        color: #64748b;
        font-size: 0.54rem;
    }

    .compact-icon-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 27px;
        width: 27px;
        height: 27px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 7px;
        text-decoration: none;
        font-size: 0.58rem;
    }

    .compact-empty {
        padding: 12px;
        color: #64748b;
        text-align: center;
        font-size: 0.62rem;
    }

    .compact-action-body {
        padding: 9px;
    }

    .compact-action-body .form-label,
    .compact-financial-box .form-label {
        margin-bottom: 3px;
        color: #334155;
        font-size: 0.61rem;
        font-weight: 700;
    }

    .compact-action-body .form-control,
    .compact-financial-box .form-control,
    .compact-financial-box .input-group-text {
        min-height: 31px;
        font-size: 0.66rem;
    }

    .compact-financial-box {
        margin-bottom: 8px;
        padding: 8px;
        background: #f0fdf4;
        border: 1px solid #bbf7d0;
        border-radius: 8px;
    }

    .compact-financial-box small {
        display: block;
        margin-top: 3px;
        color: #64748b;
        font-size: 0.54rem;
    }

    .compact-action-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 5px;
        margin-top: 7px;
    }

    .compact-danger-btn,
    .compact-moreinfo-btn {
        min-height: 30px;
        padding: 5px 9px;
        border: 0;
        border-radius: 7px;
        font-size: 0.65rem;
        font-weight: 700;
    }

    .compact-danger-btn {
        color: #991b1b;
        background: #fee2e2;
    }

    .compact-moreinfo-btn {
        color: #92400e;
        background: #fef3c7;
    }

    .timeline-panel {
        padding: 10px;
    }

    .compact-timeline {
        padding: 10px 4px 2px;
    }

    .compact-timeline-item {
        position: relative;
        display: flex;
        align-items: flex-start;
        gap: 9px;
        padding-bottom: 10px;
    }

    .compact-timeline-item:not(:last-child)::after {
        content: "";
        position: absolute;
        top: 28px;
        bottom: 0;
        left: 13px;
        width: 2px;
        background: #e2e8f0;
    }

    .compact-timeline-dot {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 28px;
        width: 28px;
        height: 28px;
        color: #ffffff;
        border-radius: 50%;
        font-size: 0.57rem;
    }

    .compact-timeline-card {
        flex: 1;
        min-width: 0;
        padding: 8px 9px;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
    }

    .compact-timeline-top {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 8px;
    }

    .compact-timeline-top > div {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 5px;
    }

    .compact-timeline-top strong {
        color: #0f172a;
        font-size: 0.65rem;
    }

    .compact-timeline-top .badge {
        font-size: 0.49rem;
    }

    .compact-timeline-top small {
        color: #64748b;
        font-size: 0.53rem;
        white-space: nowrap;
    }

    .compact-timeline-user {
        margin-top: 4px;
        color: #475569;
        font-size: 0.58rem;
    }

    .compact-timeline-comment {
        margin-top: 6px;
        padding: 6px 8px;
        color: #334155;
        background: #f8fafc;
        border-left: 3px solid #6366f1;
        border-radius: 6px;
        font-size: 0.61rem;
        white-space: pre-wrap;
    }

    .compact-timeline-movement {
        margin-top: 5px;
        color: #64748b;
        font-size: 0.54rem;
    }

    .timeline-success {
        background: #16a34a !important;
    }

    .timeline-danger {
        background: #dc2626 !important;
    }

    .timeline-warning {
        background: #f59e0b !important;
    }

    .timeline-primary {
        background: #2563eb !important;
    }

    .timeline-secondary {
        background: #64748b !important;
    }

    .approval-popup-backdrop {
        position: fixed;
        inset: 0;
        z-index: 3000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 16px;
        background: rgba(15, 23, 42, 0.55);
    }

    .approval-popup-card {
        width: 100%;
        max-width: 360px;
        padding: 20px;
        text-align: center;
        background: #ffffff;
        border-radius: 14px;
        box-shadow: 0 20px 50px rgba(15, 23, 42, 0.25);
    }

    .approval-popup-icon {
        margin-bottom: 7px;
        color: #16a34a;
        font-size: 2.2rem;
    }

    .approval-popup-card h4 {
        margin-bottom: 5px;
        color: #0f172a;
        font-size: 0.9rem;
        font-weight: 800;
    }

    .approval-popup-card p {
        margin-bottom: 14px;
        color: #64748b;
        font-size: 0.67rem;
    }

    @@media (max-width: 991.98px) {
        .approval-two-column {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 640px) {
        .approval-page-topbar {
            align-items: flex-start;
            flex-direction: column;
        }

        .approval-topbar-actions {
            width: 100%;
            justify-content: space-between;
        }

        .approval-page-heading h3 {
            white-space: normal;
        }

        .compact-info-grid {
            grid-template-columns: 1fr;
        }

        .compact-timeline-top {
            flex-direction: column;
        }
    }

    /* Compact original hero */
    .worker360-hero {
        width: calc(100% - 28px);
        max-width: 1220px;
        margin-left: auto;
        margin-right: auto;
    }

    .worker360-hero .bcc-card-body,
    .worker360-hero .worker360-hero-body,
    .worker360-hero .hero-body {
        padding-left: 14px !important;
        padding-right: 14px !important;
    }

    .worker360-hero .row {
        --bs-gutter-x: 0.75rem;
    }

    .worker360-hero h1,
    .worker360-hero h2,
    .worker360-hero h3,
    .worker360-hero .hero-title {
        font-size: 0.96rem !important;
        margin-bottom: 2px !important;
    }

    .worker360-hero p,
    .worker360-hero .hero-subtitle,
    .worker360-hero .hero-meta {
        font-size: 0.62rem !important;
        line-height: 1.35;
    }

    @@media (max-width: 767.98px) {
        .worker360-hero {
            width: 100%;
        }
    }

</style>



<style>
    .approval-details-page {
        max-width: 1480px;
        margin: 0 auto;
    }

    .compact-alert {
        margin-bottom: 10px;
        padding: 8px 10px;
        font-size: 0.72rem;
    }

    .approval-page-topbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 14px;
        margin-bottom: 10px;
        padding: 11px 13px;
        background: #ffffff;
        border: 1px solid #e5e7eb;
        border-left: 4px solid #4f46e5;
        border-radius: 12px;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.035);
    }

    .approval-page-heading,
    .approval-title {
        display: flex;
        align-items: center;
        gap: 10px;
        min-width: 0;
    }

    .approval-heading-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 38px;
        width: 38px;
        height: 38px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 10px;
        font-size: 0.8rem;
    }

    .approval-eyebrow {
        margin-bottom: 1px;
        color: #6366f1;
        font-size: 0.57rem;
        font-weight: 800;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .approval-page-heading h3 {
        max-width: 800px;
        margin: 0;
        overflow: hidden;
        color: #0f172a;
        font-size: 0.96rem;
        font-weight: 800;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .approval-heading-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 4px 10px;
        margin-top: 2px;
        color: #64748b;
        font-size: 0.59rem;
    }

    .approval-topbar-actions {
        display: flex;
        align-items: center;
        gap: 7px;
        flex-shrink: 0;
    }

    .compact-btn {
        min-height: 30px;
        padding: 5px 10px;
        font-size: 0.68rem;
    }

    .approval-status-badge {
        display: inline-flex;
        align-items: center;
        padding: 5px 9px;
        border-radius: 999px;
        font-size: 0.6rem;
        font-weight: 800;
        white-space: nowrap;
    }

    .approval-status-badge.approved {
        color: #166534;
        background: #dcfce7;
    }

    .approval-status-badge.rejected {
        color: #991b1b;
        background: #fee2e2;
    }

    .approval-status-badge.pending {
        color: #92400e;
        background: #fef3c7;
    }

    .approval-status-badge.returned {
        color: #9a3412;
        background: #ffedd5;
    }

    .approval-status-badge.neutral {
        color: #475569;
        background: #e2e8f0;
    }

    .approval-tab-card {
        overflow: hidden;
        background: #f8fafc;
        border: 1px solid #e5e7eb;
        border-radius: 12px;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.035);
    }

    .approval-tabs {
        display: flex;
        gap: 4px;
        padding: 7px 8px 0;
        background: #ffffff;
        border-bottom: 1px solid #e5e7eb;
    }

    .approval-tab {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 7px 10px;
        color: #64748b;
        background: transparent;
        border: 0;
        border-bottom: 2px solid transparent;
        font-size: 0.66rem;
        font-weight: 700;
    }

    .approval-tab.active {
        color: #4338ca;
        border-bottom-color: #4f46e5;
    }

    .approval-two-column {
        display: grid;
        grid-template-columns:
            minmax(0, 1.08fr) minmax(0, 0.92fr);
        gap: 10px;
        padding: 10px;
        align-items: start;
    }

    .approval-column {
        display: flex;
        flex-direction: column;
        gap: 10px;
        min-width: 0;
    }

    .compact-section {
        overflow: hidden;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 10px;
    }

    .compact-section-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        min-height: 34px;
        padding: 7px 9px;
        color: #4338ca;
        background: #eef2ff;
        border-bottom: 1px solid #c7d2fe;
    }

    .compact-section-header > div {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .compact-section-header span {
        color: #0f172a;
        font-size: 0.67rem;
        font-weight: 800;
    }

    .compact-section-header i {
        font-size: 0.64rem;
    }

    .compact-section-header.financial {
        color: #166534;
        background: #f0fdf4;
        border-bottom-color: #bbf7d0;
    }

    .compact-section-header.workflow {
        color: #6d28d9;
        background: #f5f3ff;
        border-bottom-color: #ddd6fe;
    }

    .compact-section-header.attachments {
        color: #0369a1;
        background: #f0f9ff;
        border-bottom-color: #bae6fd;
    }

    .compact-section-header.action {
        color: #92400e;
        background: #fffbeb;
        border-bottom-color: #fde68a;
    }

    .compact-section-header.returned {
        color: #9a3412;
        background: #fff7ed;
        border-bottom-color: #fed7aa;
    }

    .compact-section-header.approval-sought {
        color: #166534;
        background: #f0fdf4;
        border-bottom-color: #bbf7d0;
    }

    .section-count {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 22px;
        height: 22px;
        padding: 0 6px;
        color: #ffffff !important;
        background: #0284c7;
        border-radius: 999px;
        font-size: 0.56rem !important;
    }

    .compact-info-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0;
        padding: 4px 9px;
    }

    .compact-info-item {
        min-width: 0;
        padding: 6px 6px;
        border-bottom: 1px solid #f1f5f9;
    }

    .compact-info-item span,
    .compact-info-item strong {
        display: block;
    }

    .compact-info-item span {
        margin-bottom: 1px;
        color: #64748b;
        font-size: 0.55rem;
        font-weight: 700;
        text-transform: uppercase;
    }

    .compact-info-item strong {
        overflow-wrap: anywhere;
        color: #0f172a;
        font-size: 0.66rem;
        font-weight: 700;
    }

    .compact-rich-box {
        min-height: 46px;
        padding: 9px 10px;
        color: #334155;
        font-size: 0.67rem;
        line-height: 1.45;
        overflow-wrap: anywhere;
    }

    .compact-rich-box p:last-child {
        margin-bottom: 0;
    }

    .compact-workflow {
        padding: 7px 9px;
    }

    .compact-workflow-row {
        position: relative;
        display: flex;
        align-items: flex-start;
        gap: 8px;
        min-height: 38px;
        padding-bottom: 7px;
    }

    .compact-workflow-row:not(:last-child)::after {
        content: "";
        position: absolute;
        top: 24px;
        bottom: -2px;
        left: 10px;
        width: 2px;
        background: #e2e8f0;
    }

    .workflow-dot {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 22px;
        width: 22px;
        height: 22px;
        color: #64748b;
        background: #e2e8f0;
        border-radius: 50%;
        font-size: 0.52rem;
    }

    .compact-workflow-row.completed .workflow-dot {
        color: #ffffff;
        background: #16a34a;
    }

    .compact-workflow-row.current .workflow-dot {
        color: #ffffff;
        background: #f59e0b;
    }

    .compact-workflow-row.rejected .workflow-dot {
        color: #ffffff;
        background: #dc2626;
    }

    .compact-workflow-row.returned .workflow-dot {
        color: #ffffff;
        background: #f97316;
    }

    .compact-workflow-row strong,
    .compact-workflow-row small {
        display: block;
    }

    .compact-workflow-row strong {
        color: #0f172a;
        font-size: 0.64rem;
    }

    .compact-workflow-row small {
        margin-top: 1px;
        color: #64748b;
        font-size: 0.56rem;
        line-height: 1.35;
    }

    .compact-attachment-list {
        padding: 5px 8px;
    }

    .compact-attachment {
        display: flex;
        align-items: center;
        gap: 7px;
        padding: 6px 2px;
        border-bottom: 1px solid #f1f5f9;
    }

    .attachment-icon {
        flex: 0 0 24px;
        font-size: 0.8rem;
        text-align: center;
    }

    .attachment-details {
        min-width: 0;
        flex: 1;
    }

    .attachment-details strong,
    .attachment-details small {
        display: block;
    }

    .attachment-details strong {
        overflow: hidden;
        color: #0f172a;
        font-size: 0.62rem;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .attachment-details small {
        color: #64748b;
        font-size: 0.54rem;
    }

    .compact-icon-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 27px;
        width: 27px;
        height: 27px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 7px;
        text-decoration: none;
        font-size: 0.58rem;
    }

    .compact-empty {
        padding: 12px;
        color: #64748b;
        text-align: center;
        font-size: 0.62rem;
    }

    .compact-action-body {
        padding: 9px;
    }

    .compact-action-body .form-label,
    .compact-financial-box .form-label {
        margin-bottom: 3px;
        color: #334155;
        font-size: 0.61rem;
        font-weight: 700;
    }

    .compact-action-body .form-control,
    .compact-financial-box .form-control,
    .compact-financial-box .input-group-text {
        min-height: 31px;
        font-size: 0.66rem;
    }

    .compact-financial-box {
        margin-bottom: 8px;
        padding: 8px;
        background: #f0fdf4;
        border: 1px solid #bbf7d0;
        border-radius: 8px;
    }

    .compact-financial-box small {
        display: block;
        margin-top: 3px;
        color: #64748b;
        font-size: 0.54rem;
    }

    .compact-action-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 5px;
        margin-top: 7px;
    }

    .compact-danger-btn,
    .compact-moreinfo-btn {
        min-height: 30px;
        padding: 5px 9px;
        border: 0;
        border-radius: 7px;
        font-size: 0.65rem;
        font-weight: 700;
    }

    .compact-danger-btn {
        color: #991b1b;
        background: #fee2e2;
    }

    .compact-moreinfo-btn {
        color: #92400e;
        background: #fef3c7;
    }

    .timeline-panel {
        padding: 10px;
    }

    .compact-timeline {
        padding: 10px 4px 2px;
    }

    .compact-timeline-item {
        position: relative;
        display: flex;
        align-items: flex-start;
        gap: 9px;
        padding-bottom: 10px;
    }

    .compact-timeline-item:not(:last-child)::after {
        content: "";
        position: absolute;
        top: 28px;
        bottom: 0;
        left: 13px;
        width: 2px;
        background: #e2e8f0;
    }

    .compact-timeline-dot {
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 28px;
        width: 28px;
        height: 28px;
        color: #ffffff;
        border-radius: 50%;
        font-size: 0.57rem;
    }

    .compact-timeline-card {
        flex: 1;
        min-width: 0;
        padding: 8px 9px;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
    }

    .compact-timeline-top {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 8px;
    }

    .compact-timeline-top > div {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 5px;
    }

    .compact-timeline-top strong {
        color: #0f172a;
        font-size: 0.65rem;
    }

    .compact-timeline-top .badge {
        font-size: 0.49rem;
    }

    .compact-timeline-top small {
        color: #64748b;
        font-size: 0.53rem;
        white-space: nowrap;
    }

    .compact-timeline-user {
        margin-top: 4px;
        color: #475569;
        font-size: 0.58rem;
    }

    .compact-timeline-comment {
        margin-top: 6px;
        padding: 6px 8px;
        color: #334155;
        background: #f8fafc;
        border-left: 3px solid #6366f1;
        border-radius: 6px;
        font-size: 0.61rem;
        white-space: pre-wrap;
    }

    .compact-timeline-movement {
        margin-top: 5px;
        color: #64748b;
        font-size: 0.54rem;
    }

    .timeline-success {
        background: #16a34a !important;
    }

    .timeline-danger {
        background: #dc2626 !important;
    }

    .timeline-warning {
        background: #f59e0b !important;
    }

    .timeline-primary {
        background: #2563eb !important;
    }

    .timeline-secondary {
        background: #64748b !important;
    }

    .approval-popup-backdrop {
        position: fixed;
        inset: 0;
        z-index: 3000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 16px;
        background: rgba(15, 23, 42, 0.55);
    }

    .approval-popup-card {
        width: 100%;
        max-width: 360px;
        padding: 20px;
        text-align: center;
        background: #ffffff;
        border-radius: 14px;
        box-shadow: 0 20px 50px rgba(15, 23, 42, 0.25);
    }

    .approval-popup-icon {
        margin-bottom: 7px;
        color: #16a34a;
        font-size: 2.2rem;
    }

    .approval-popup-card h4 {
        margin-bottom: 5px;
        color: #0f172a;
        font-size: 0.9rem;
        font-weight: 800;
    }

    .approval-popup-card p {
        margin-bottom: 14px;
        color: #64748b;
        font-size: 0.67rem;
    }

    @@media (max-width: 991.98px) {
        .approval-two-column {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 640px) {
        .approval-page-topbar {
            align-items: flex-start;
            flex-direction: column;
        }

        .approval-topbar-actions {
            width: 100%;
            justify-content: space-between;
        }

        .approval-page-heading h3 {
            white-space: normal;
        }

        .compact-info-grid {
            grid-template-columns: 1fr;
        }

        .compact-timeline-top {
            flex-direction: column;
        }
    }
</style>


