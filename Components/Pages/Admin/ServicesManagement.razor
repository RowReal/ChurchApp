@page "/admin/services"
@layout Components.Layout.MainLayout

@using ChurchApp.Models
@using ChurchApp.Services

@inject ExcuseService ExcuseService
@inject AuthService AuthService
@inject NavigationManager Navigation

<PageTitle>Service Management - BCC ServiceHub</PageTitle>

@if (!AuthService.IsAuthenticated)
{
    <div class="bcc-empty">
        <i class="fas fa-lock"></i>
        <h4>Please Log In</h4>
        <p>You need to be logged in to access Service Management.</p>

        <a href="/login"
           class="bcc-btn bcc-btn-primary">
            Go to Login
        </a>
    </div>
}
else if (!isAuthorized)
{
    <div class="bcc-empty">
        <i class="fas fa-ban"></i>
        <h4>Access Denied</h4>
        <p>
            Service Management is available to authorised church leadership
            and administrators.
        </p>

        <a href="/"
           class="bcc-btn bcc-btn-primary">
            Return to Dashboard
        </a>
    </div>
}
else
{
    <div class="worker360-page service-management-page">

        @if (!string.IsNullOrWhiteSpace(errorMessage) &&
             !showServiceModal)
        {
            <div class="alert alert-danger py-2">
                <i class="fas fa-exclamation-circle me-2"></i>
                @errorMessage
            </div>
        }

        <div class="bcc-page-header service-management-header">
            <div>
                <div class="bcc-page-subtitle">
                    <i class="fas fa-calendar-check me-2"></i>
                    Worker Management
                </div>

                <div class="bcc-page-title">
                    Service Management
                </div>

                <div class="bcc-page-subtitle">
                    Create, schedule and manage church services.
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="button"
                        class="bcc-btn profile-form-secondary-btn compact-btn"
                        @onclick="GoBack">
                    <i class="fas fa-arrow-left me-2"></i>
                    Back
                </button>

                <button type="button"
                        class="bcc-btn bcc-btn-primary compact-btn"
                        @onclick="ShowAddServiceModal">
                    <i class="fas fa-plus me-2"></i>
                    Add Service
                </button>
            </div>
        </div>

        <div class="row g-3 align-items-start">

            <div class="col-12 col-xl-4">
                <div class="bcc-card service-filter-card">

                    <div class="service-panel-header">
                        <div class="service-panel-icon">
                            <i class="fas fa-filter"></i>
                        </div>

                        <div>
                            <h6>Service Filters</h6>
                            <p>Summary and search controls.</p>
                        </div>
                    </div>

                    <div class="bcc-card-body p-3">

                        <div class="service-summary-grid mb-3">

                            <div>
                                <span>Total</span>
                                <strong>@allServices.Count</strong>
                            </div>

                            <div class="active">
                                <span>Active</span>
                                <strong>@allServices.Count(x => x.IsActive)</strong>
                            </div>

                            <div class="inactive">
                                <span>Inactive</span>
                                <strong>@allServices.Count(x => !x.IsActive)</strong>
                            </div>

                            <div class="result">
                                <span>Filtered</span>
                                <strong>@filteredServices.Count</strong>
                            </div>

                        </div>

                        <div class="service-filter-tip">
                            <i class="fas fa-lightbulb"></i>

                            <span>
                                Filter services by status and recurrence pattern.
                            </span>
                        </div>

                        <div class="row g-2">

                            <div class="col-12">
                                <label class="form-label compact-label">
                                    Status
                                </label>

                                <select value="@selectedStatusFilter"
                                        @onchange='@(e => OnFilterChanged(e, "status"))'
                                        class="form-select form-select-sm">
                                    <option value="all">All Services</option>
                                    <option value="active">Active Only</option>
                                    <option value="inactive">Inactive Only</option>
                                </select>
                            </div>

                            <div class="col-12">
                                <label class="form-label compact-label">
                                    Recurrence
                                </label>

                                <select value="@selectedRecurrenceFilter"
                                        @onchange='@(e => OnFilterChanged(e, "recurrence"))'
                                        class="form-select form-select-sm">
                                    <option value="all">All Types</option>
                                    <option value="Weekly">Weekly</option>
                                    <option value="Monthly">Monthly</option>
                                    <option value="OneTime">One Time</option>
                                </select>
                            </div>

                        </div>

                        <div class="mt-3 d-flex justify-content-end gap-2">
                            <button type="button"
                                    class="bcc-btn profile-form-secondary-btn compact-btn"
                                    @onclick="ResetFilters">
                                <i class="fas fa-rotate-left me-1"></i>
                                Reset
                            </button>

                            <button type="button"
                                    class="bcc-btn bcc-btn-primary compact-btn"
                                    @onclick="ShowAddServiceModal">
                                <i class="fas fa-plus me-1"></i>
                                Add
                            </button>
                        </div>

                    </div>
                </div>
            </div>

            <div class="col-12 col-xl-8">
                <div class="bcc-card service-list-card">

                    <div class="service-list-header">
                        <div>
                            <h6>
                                <i class="fas fa-list me-2"></i>
                                Service Register
                            </h6>

                            <p>
                                @filteredServices.Count service(s) match the current filters.
                            </p>
                        </div>

                        <span class="service-result-count">
                            @filteredServices.Count
                        </span>
                    </div>

                    <div class="bcc-card-body p-0">

                        @if (filteredServices.Any())
                        {
                            <div class="service-list">

                                @foreach (var service in filteredServices)
                                {
                                    <div class="service-row @(service.IsActive ? "" : "inactive-row")">

                                        <div class="service-row-main">

                                            <div class="service-row-icon">
                                                <i class="fas fa-calendar-day"></i>
                                            </div>

                                            <div class="service-row-content">
                                                <div class="service-row-title">
                                                    <strong>@service.Name</strong>

                                                    <span class="service-type-badge">
                                                        @service.RecurrencePattern
                                                    </span>
                                                </div>

                                                @if (!string.IsNullOrWhiteSpace(service.Description))
                                                {
                                                    <p>@service.Description</p>
                                                }

                                                <div class="service-row-meta">
                                                    <span>
                                                        <i class="fas fa-calendar"></i>
                                                        @GetServiceScheduleDisplay(service)
                                                    </span>

                                                    <span>
                                                        <i class="fas fa-clock"></i>
                                                        @GetServiceTimeDisplay(service)
                                                    </span>
                                                </div>
                                            </div>

                                        </div>

                                        <div class="service-row-side">

                                            <span class="service-status-badge
                                                @(service.IsActive ? "active" : "inactive")">
                                                <i class="fas @(service.IsActive
                                                    ? "fa-circle-check"
                                                    : "fa-circle-pause")"></i>

                                                @(service.IsActive
                                                    ? "Active"
                                                    : "Inactive")
                                            </span>

                                            <div class="service-row-actions">
                                                <button type="button"
                                                        class="service-action-btn edit"
                                                        title="Edit service"
                                                        @onclick="() => EditService(service)">
                                                    <i class="fas fa-pen"></i>
                                                </button>

                                                @if (service.IsActive)
                                                {
                                                    <button type="button"
                                                            class="service-action-btn deactivate"
                                                            title="Deactivate service"
                                                            @onclick="() => DeactivateService(service)">
                                                        <i class="fas fa-pause"></i>
                                                    </button>
                                                }
                                                else
                                                {
                                                    <button type="button"
                                                            class="service-action-btn activate"
                                                            title="Activate service"
                                                            @onclick="() => ActivateService(service)">
                                                        <i class="fas fa-play"></i>
                                                    </button>
                                                }
                                            </div>

                                        </div>

                                    </div>
                                }

                            </div>
                        }
                        else
                        {
                            <div class="bcc-empty py-4">
                                <i class="fas fa-calendar-xmark"></i>
                                <h4>No Services Found</h4>
                                <p>No service matches the current filters.</p>

                                <button type="button"
                                        class="bcc-btn bcc-btn-primary compact-btn"
                                        @onclick="ResetFilters">
                                    Reset Filters
                                </button>
                            </div>
                        }

                    </div>
                </div>
            </div>

        </div>
    </div>
}

@if (showServiceModal)
{
    <div class="service-modal-backdrop"
         @onclick="CloseServiceModal">

        <div class="service-modal-card"
             @onclick:stopPropagation="true">

            <div class="service-modal-header">
                <div>
                    <h4>
                        @(editingService
                            ? "Edit Service"
                            : "Add Service")
                    </h4>

                    <p>
                        Configure the service schedule and status.
                    </p>
                </div>

                <button type="button"
                        class="btn-close"
                        @onclick="CloseServiceModal">
                </button>
            </div>

            <EditForm Model="@serviceModel"
                      OnValidSubmit="@SaveService">

                <DataAnnotationsValidator />

                <div class="service-modal-body">

                    <div class="row g-2">

                        <div class="col-12 col-md-7">
                            <label class="form-label compact-label">
                                Service Name
                                <span class="text-danger">*</span>
                            </label>

                            <InputText @bind-Value="serviceModel.Name"
                                       class="form-control form-control-sm"
                                       placeholder="e.g. Sunday Service" />

                            <ValidationMessage For="@(() => serviceModel.Name)"
                                               class="text-danger small" />
                        </div>

                        <div class="col-12 col-md-5">
                            <label class="form-label compact-label">
                                Recurrence Pattern
                                <span class="text-danger">*</span>
                            </label>

                            <InputSelect @bind-Value="serviceModel.RecurrencePattern"
                                         class="form-select form-select-sm">
                                <option value="OneTime">One Time</option>
                                <option value="Weekly">Weekly</option>
                                <option value="Monthly">Monthly</option>
                            </InputSelect>
                        </div>

                        <div class="col-12">
                            <label class="form-label compact-label">
                                Description
                            </label>

                            <InputTextArea @bind-Value="serviceModel.Description"
                                           class="form-control form-control-sm"
                                           rows="2"
                                           placeholder="Brief description of the service" />
                        </div>

                    </div>

                    @if (serviceModel.RecurrencePattern == "OneTime")
                    {
                        <div class="service-schedule-box mt-3">

                            <div class="row g-2">

                                <div class="col-12 col-md-6">
                                    <label class="form-label compact-label">
                                        Service Date
                                        <span class="text-danger">*</span>
                                    </label>

                                    <InputDate @bind-Value="serviceModel.SpecificDate"
                                               class="form-control form-control-sm" />
                                </div>

                                <div class="col-6 col-md-3">
                                    <label class="form-label compact-label">
                                        Start Time
                                    </label>

                                    <InputText @bind-Value="specificStartTimeString"
                                               class="form-control form-control-sm"
                                               type="time" />
                                </div>

                                <div class="col-6 col-md-3">
                                    <label class="form-label compact-label">
                                        End Time
                                    </label>

                                    <InputText @bind-Value="specificEndTimeString"
                                               class="form-control form-control-sm"
                                               type="time" />
                                </div>

                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="service-schedule-box mt-3">

                            <div class="row g-2">

                                @if (serviceModel.RecurrencePattern == "Weekly")
                                {
                                    <div class="col-12 col-md-6">
                                        <label class="form-label compact-label">
                                            Day of Week
                                        </label>

                                        <InputSelect @bind-Value="dayOfWeekString"
                                                     class="form-select form-select-sm">
                                            <option value="0">Sunday</option>
                                            <option value="1">Monday</option>
                                            <option value="2">Tuesday</option>
                                            <option value="3">Wednesday</option>
                                            <option value="4">Thursday</option>
                                            <option value="5">Friday</option>
                                            <option value="6">Saturday</option>
                                        </InputSelect>
                                    </div>
                                }

                                @if (serviceModel.RecurrencePattern == "Monthly")
                                {
                                    <div class="col-12 col-md-6">
                                        <label class="form-label compact-label">
                                            Week of Month
                                        </label>

                                        <InputSelect @bind-Value="serviceModel.WeekOfMonth"
                                                     class="form-select form-select-sm">
                                            <option value="1">First Week</option>
                                            <option value="2">Second Week</option>
                                            <option value="3">Third Week</option>
                                            <option value="4">Fourth Week</option>
                                            <option value="5">Last Week</option>
                                        </InputSelect>
                                    </div>

                                    <div class="col-12 col-md-6">
                                        <label class="form-label compact-label">
                                            Day of Week
                                        </label>

                                        <InputSelect @bind-Value="dayOfWeekString"
                                                     class="form-select form-select-sm">
                                            <option value="0">Sunday</option>
                                            <option value="1">Monday</option>
                                            <option value="2">Tuesday</option>
                                            <option value="3">Wednesday</option>
                                            <option value="4">Thursday</option>
                                            <option value="5">Friday</option>
                                            <option value="6">Saturday</option>
                                        </InputSelect>
                                    </div>
                                }

                                <div class="col-6 col-md-3">
                                    <label class="form-label compact-label">
                                        Start Time
                                    </label>

                                    <InputText @bind-Value="startTimeString"
                                               class="form-control form-control-sm"
                                               type="time" />
                                </div>

                                <div class="col-6 col-md-3">
                                    <label class="form-label compact-label">
                                        End Time
                                    </label>

                                    <InputText @bind-Value="endTimeString"
                                               class="form-control form-control-sm"
                                               type="time" />
                                </div>

                            </div>
                        </div>
                    }

                    <div class="service-active-switch mt-3">
                        <div>
                            <strong>Active Service</strong>
                            <span>
                                Active services are available throughout the application.
                            </span>
                        </div>

                        <div class="form-check form-switch">
                            <InputCheckbox @bind-Value="serviceModel.IsActive"
                                           class="form-check-input" />
                        </div>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(errorMessage))
                    {
                        <div class="alert alert-danger py-2 mt-3">
                            <i class="fas fa-exclamation-circle me-2"></i>
                            @errorMessage
                        </div>
                    }

                </div>

                <div class="service-modal-footer">
                    <button type="button"
                            class="bcc-btn profile-form-secondary-btn compact-btn"
                            disabled="@isLoading"
                            @onclick="CloseServiceModal">
                        Cancel
                    </button>

                    <button type="submit"
                            class="bcc-btn bcc-btn-primary compact-btn"
                            disabled="@isLoading">

                        @if (isLoading)
                        {
                            <span class="spinner-border spinner-border-sm me-2"></span>
                            <span>Saving...</span>
                        }
                        else
                        {
                            <i class="fas fa-save me-2"></i>

                            <span>
                                @(editingService
                                    ? "Update Service"
                                    : "Create Service")
                            </span>
                        }

                    </button>
                </div>

            </EditForm>

        </div>
    </div>
}


@code {
    private List<Service> allServices = new();
    private List<Service> filteredServices = new();
    private bool showServiceModal = false;
    private bool editingService = false;
    private bool isLoading = false;
    private ServiceModel serviceModel = new();
    private Service? currentService;

    // Filter variables
    private string selectedStatusFilter = "all";
    private string selectedRecurrenceFilter = "all";

    // Use separate string variables for form inputs
    private string startTimeString = "09:00";
    private string endTimeString = "10:00";
    private string specificStartTimeString = "09:00";
    private string specificEndTimeString = "10:00";
    private string dayOfWeekString = "0";
    private string errorMessage = string.Empty;
    private bool isAuthorized = false;

    protected override async Task OnInitializedAsync()
    {
        // Check authorization
        if (!IsUserAuthorizedForServiceManagement())
        {
            isAuthorized = false;
            return;
        }
        
        isAuthorized = true;
        await LoadServices();
    }

    private bool IsUserAuthorizedForServiceManagement()
    {
        if (!AuthService.IsAuthenticated || AuthService.CurrentWorker == null)
            return false;

        var role = AuthService.CurrentWorker.Role?.ToLowerInvariant() ?? "";
        
        // Check specific roles that can manage services
        if (role.Contains("church admin") || 
            role.Contains("pastor in charge") ||
            role.Contains("head of service") ||
            role.Contains("assistant head of service") ||
            role.Contains("asst head of service"))
        {
            return true;
        }
        
        // Also check the CanAccessAdminPanel property as fallback
        if (AuthService.CurrentWorker.CanAccessAdminPanel)
            return true;
            
        return false;
    }

    private async Task LoadServices()
    {
        try
        {
            allServices = await ExcuseService.GetAllServicesAsync();
            ApplyFilters();
        }
        catch (Exception ex)
        {
            errorMessage = $"Error loading services: {ex.Message}";
        }
    }

    private void ApplyFilters()
    {
        var filtered = allServices.AsEnumerable();

        if (selectedStatusFilter == "active")
            filtered = filtered.Where(s => s.IsActive);
        else if (selectedStatusFilter == "inactive")
            filtered = filtered.Where(s => !s.IsActive);

        if (selectedRecurrenceFilter != "all")
            filtered = filtered.Where(s => s.RecurrencePattern == selectedRecurrenceFilter);

        filteredServices = filtered.ToList();
        StateHasChanged();
    }

    private void ResetFilters()
    {
        selectedStatusFilter = "all";
        selectedRecurrenceFilter = "all";
        ApplyFilters();
    }

    private string GetServiceScheduleDisplay(Service service)
    {
        if (service.RecurrencePattern == "Weekly")
        {
            var dayName = GetDayName(service.DayOfWeek);
            return $"Every {dayName}";
        }
        else if (service.RecurrencePattern == "Monthly")
        {
            var dayName = GetDayName(service.DayOfWeek);
            var weekDisplay = service.WeekOfMonth switch
            {
                1 => "First",
                2 => "Second",
                3 => "Third",
                4 => "Fourth",
                5 => "Last",
                _ => "Week " + service.WeekOfMonth
            };
            return $"{weekDisplay} {dayName}";
        }
        else if (service.RecurrencePattern == "OneTime")
        {
            return service.SpecificDate?.ToString("MMM dd, yyyy") ?? "One Time";
        }
        return service.RecurrencePattern;
    }

    private string GetDayName(DayOfWeek? dayOfWeek)
    {
        return dayOfWeek switch
        {
            DayOfWeek.Sunday => "Sun",
            DayOfWeek.Monday => "Mon",
            DayOfWeek.Tuesday => "Tue",
            DayOfWeek.Wednesday => "Wed",
            DayOfWeek.Thursday => "Thu",
            DayOfWeek.Friday => "Fri",
            DayOfWeek.Saturday => "Sat",
            _ => "Unknown"
        };
    }

    private void ShowAddServiceModal()
    {
        serviceModel = new ServiceModel
        {
            IsActive = true,
            RecurrencePattern = "OneTime"
        };
        editingService = false;
        showServiceModal = true;
        errorMessage = string.Empty;

        // Reset form values
        startTimeString = "09:00";
        endTimeString = "10:00";
        specificStartTimeString = "09:00";
        specificEndTimeString = "10:00";
        dayOfWeekString = "0";
    }

    private void EditService(Service service)
    {
        currentService = service;
        serviceModel = new ServiceModel
        {
            Name = service.Name,
            Description = service.Description,
            RecurrencePattern = service.RecurrencePattern,
            WeekOfMonth = service.WeekOfMonth,
            StartTime = service.StartTime,
            EndTime = service.EndTime,
            SpecificDate = service.SpecificDate,
            SpecificStartTime = service.SpecificStartTime,
            SpecificEndTime = service.SpecificEndTime,
            IsActive = service.IsActive
        };

        dayOfWeekString = service.DayOfWeek.HasValue ? ((int)service.DayOfWeek.Value).ToString() : "0";
        startTimeString = service.StartTime?.ToString(@"hh\:mm") ?? "09:00";
        endTimeString = service.EndTime?.ToString(@"hh\:mm") ?? "10:00";
        specificStartTimeString = service.SpecificStartTime?.ToString(@"hh\:mm") ?? "09:00";
        specificEndTimeString = service.SpecificEndTime?.ToString(@"hh\:mm") ?? "10:00";

        editingService = true;
        showServiceModal = true;
        errorMessage = string.Empty;
    }

    private void CloseServiceModal()
    {
        showServiceModal = false;
        currentService = null;
        errorMessage = string.Empty;
    }

    private async Task SaveService()
    {
        isLoading = true;
        errorMessage = string.Empty;

        try
        {
            if (TimeSpan.TryParse(startTimeString, out var startTime))
                serviceModel.StartTime = startTime;
            if (TimeSpan.TryParse(endTimeString, out var endTime))
                serviceModel.EndTime = endTime;
            if (TimeSpan.TryParse(specificStartTimeString, out var specificStartTime))
                serviceModel.SpecificStartTime = specificStartTime;
            if (TimeSpan.TryParse(specificEndTimeString, out var specificEndTime))
                serviceModel.SpecificEndTime = specificEndTime;

            if (editingService && currentService != null)
            {
                currentService.Name = serviceModel.Name;
                currentService.Description = serviceModel.Description;
                currentService.RecurrencePattern = serviceModel.RecurrencePattern;
                currentService.DayOfWeek = int.TryParse(dayOfWeekString, out int dayInt) ? (DayOfWeek)dayInt : DayOfWeek.Sunday;
                currentService.WeekOfMonth = serviceModel.WeekOfMonth;
                currentService.StartTime = serviceModel.StartTime;
                currentService.EndTime = serviceModel.EndTime;
                currentService.SpecificDate = serviceModel.SpecificDate;
                currentService.SpecificStartTime = serviceModel.SpecificStartTime;
                currentService.SpecificEndTime = serviceModel.SpecificEndTime;
                currentService.IsActive = serviceModel.IsActive;
                currentService.LastUpdated = DateTime.UtcNow;

                await ExcuseService.UpdateServiceAsync(currentService);
            }
            else
            {
                var service = new Service
                {
                    Name = serviceModel.Name,
                    Description = serviceModel.Description,
                    RecurrencePattern = serviceModel.RecurrencePattern,
                    DayOfWeek = int.TryParse(dayOfWeekString, out int dayInt) ? (DayOfWeek)dayInt : DayOfWeek.Sunday,
                    WeekOfMonth = serviceModel.WeekOfMonth,
                    StartTime = serviceModel.StartTime,
                    EndTime = serviceModel.EndTime,
                    SpecificDate = serviceModel.SpecificDate,
                    SpecificStartTime = serviceModel.SpecificStartTime,
                    SpecificEndTime = serviceModel.SpecificEndTime,
                    IsActive = serviceModel.IsActive,
                    CreatedDate = DateTime.UtcNow
                };

                await ExcuseService.CreateServiceAsync(service);
            }

            CloseServiceModal();
            await LoadServices();
        }
        catch (Exception ex)
        {
            errorMessage = $"Error saving service: {ex.Message}";
        }

        isLoading = false;
    }

    private async Task DeactivateService(Service service)
    {
        if (await ExcuseService.DeleteServiceAsync(service.Id))
        {
            await LoadServices();
        }
    }

    private async Task ActivateService(Service service)
    {
        service.IsActive = true;
        service.LastUpdated = DateTime.UtcNow;
        await ExcuseService.UpdateServiceAsync(service);
        await LoadServices();
    }

    private void OnFilterChanged(ChangeEventArgs e, string filterType)
    {
        var newValue = e.Value?.ToString() ?? "all";

        if (filterType == "status")
            selectedStatusFilter = newValue;
        else if (filterType == "recurrence")
            selectedRecurrenceFilter = newValue;

        ApplyFilters();
    }

    private string GetServiceTimeDisplay(Service service)
    {
        var start =
            service.RecurrencePattern == "OneTime"
                ? service.SpecificStartTime
                : service.StartTime;

        var end =
            service.RecurrencePattern == "OneTime"
                ? service.SpecificEndTime
                : service.EndTime;

        if (!start.HasValue)
            return "Time not set";

        return end.HasValue
            ? $"{start.Value:hh\\:mm} - {end.Value:hh\\:mm}"
            : $"{start.Value:hh\\:mm}";
    }

    private void GoBack()
    {
        Navigation.NavigateTo(
            "/workspace/worker-management");
    }
}


<style>
    .service-management-page {
        max-width: 1500px;
        margin: 0 auto;
    }

    .service-management-header {
        margin-bottom: 14px;
    }

    .compact-btn {
        min-height: 32px;
        padding: 6px 11px;
        font-size: 0.78rem;
    }

    .service-filter-card,
    .service-list-card {
        overflow: hidden;
        border: 1px solid #e5e7eb;
        border-radius: 13px;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.035);
    }

    .service-filter-card {
        border-top: 4px solid #4f46e5;
    }

    .service-list-card {
        border-top: 4px solid #7c3aed;
    }

    .service-panel-header,
    .service-list-header {
        display: flex;
        align-items: center;
        gap: 9px;
        padding: 10px 12px;
        background: #ffffff;
        border-bottom: 1px solid #eef2f7;
    }

    .service-list-header {
        justify-content: space-between;
    }

    .service-panel-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 32px;
        width: 32px;
        height: 32px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 9px;
        font-size: 0.74rem;
    }

    .service-panel-header h6,
    .service-list-header h6 {
        margin: 0 0 1px;
        color: #0f172a;
        font-size: 0.82rem;
        font-weight: 800;
    }

    .service-panel-header p,
    .service-list-header p {
        margin: 0;
        color: #64748b;
        font-size: 0.64rem;
    }

    .service-result-count {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 29px;
        height: 29px;
        padding: 0 8px;
        color: #ffffff;
        background: #4f46e5;
        border-radius: 999px;
        font-size: 0.7rem;
        font-weight: 800;
    }

    .service-summary-grid {
        display: grid;
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
        gap: 7px;
    }

    .service-summary-grid > div {
        padding: 8px 9px;
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
    }

    .service-summary-grid span,
    .service-summary-grid strong {
        display: block;
    }

    .service-summary-grid span {
        color: #64748b;
        font-size: 0.58rem;
        font-weight: 700;
        text-transform: uppercase;
    }

    .service-summary-grid strong {
        margin-top: 1px;
        color: #0f172a;
        font-size: 0.88rem;
    }

    .service-summary-grid .active {
        background: #f0fdf4;
        border-color: #bbf7d0;
    }

    .service-summary-grid .active strong {
        color: #166534;
    }

    .service-summary-grid .inactive {
        background: #fef2f2;
        border-color: #fecaca;
    }

    .service-summary-grid .inactive strong {
        color: #991b1b;
    }

    .service-summary-grid .result {
        background: #eef2ff;
        border-color: #c7d2fe;
    }

    .service-summary-grid .result strong {
        color: #4338ca;
    }

    .service-filter-tip {
        display: flex;
        align-items: flex-start;
        gap: 7px;
        margin-bottom: 11px;
        padding: 8px 9px;
        color: #4338ca;
        background: #eef2ff;
        border: 1px solid #c7d2fe;
        border-radius: 8px;
        font-size: 0.62rem;
    }

    .compact-label {
        margin-bottom: 3px;
        color: #475569;
        font-size: 0.67rem;
        font-weight: 700;
    }

    .service-management-page .form-control,
    .service-management-page .form-select,
    .service-modal-card .form-control,
    .service-modal-card .form-select {
        min-height: 32px;
        font-size: 0.75rem;
    }

    .service-list {
        display: flex;
        flex-direction: column;
        max-height: 670px;
        overflow-y: auto;
    }

    .service-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        padding: 10px 12px;
        background: #ffffff;
        border-bottom: 1px solid #eef2f7;
    }

    .service-row:hover {
        background: #fafafa;
    }

    .service-row.inactive-row {
        background: #f8fafc;
        opacity: 0.78;
    }

    .service-row-main {
        display: flex;
        align-items: center;
        gap: 10px;
        min-width: 0;
    }

    .service-row-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        flex: 0 0 38px;
        width: 38px;
        height: 38px;
        color: #4338ca;
        background: #eef2ff;
        border-radius: 10px;
        font-size: 0.76rem;
    }

    .service-row-content {
        min-width: 0;
    }

    .service-row-title {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 6px;
    }

    .service-row-title strong {
        color: #0f172a;
        font-size: 0.74rem;
    }

    .service-type-badge {
        display: inline-flex;
        padding: 3px 6px;
        color: #0369a1;
        background: #e0f2fe;
        border-radius: 999px;
        font-size: 0.57rem;
        font-weight: 800;
    }

    .service-row-content p {
        margin: 3px 0 4px;
        color: #64748b;
        font-size: 0.62rem;
        line-height: 1.35;
    }

    .service-row-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 5px 12px;
    }

    .service-row-meta span {
        color: #64748b;
        font-size: 0.59rem;
    }

    .service-row-meta i {
        margin-right: 4px;
        color: #4f46e5;
    }

    .service-row-side {
        display: flex;
        align-items: center;
        gap: 8px;
        flex-shrink: 0;
    }

    .service-status-badge {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 4px 7px;
        border-radius: 999px;
        font-size: 0.59rem;
        font-weight: 800;
        white-space: nowrap;
    }

    .service-status-badge.active {
        color: #166534;
        background: #dcfce7;
    }

    .service-status-badge.inactive {
        color: #64748b;
        background: #e2e8f0;
    }

    .service-row-actions {
        display: inline-flex;
        gap: 4px;
    }

    .service-action-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 28px;
        height: 28px;
        border: 0;
        border-radius: 7px;
        font-size: 0.62rem;
    }

    .service-action-btn.edit {
        color: #4338ca;
        background: #eef2ff;
    }

    .service-action-btn.deactivate {
        color: #991b1b;
        background: #fee2e2;
    }

    .service-action-btn.activate {
        color: #166534;
        background: #dcfce7;
    }

    .service-modal-backdrop {
        position: fixed;
        inset: 0;
        z-index: 5000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 14px;
        background: rgba(15, 23, 42, 0.58);
        backdrop-filter: blur(2px);
    }

    .service-modal-card {
        width: 100%;
        max-width: 760px;
        max-height: 92vh;
        overflow-y: auto;
        background: #ffffff;
        border-radius: 14px;
        box-shadow: 0 22px 55px rgba(15, 23, 42, 0.28);
    }

    .service-modal-header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 10px;
        padding: 14px 16px 10px;
        border-bottom: 1px solid #eef2f7;
    }

    .service-modal-header h4 {
        margin: 0 0 2px;
        color: #0f172a;
        font-size: 0.95rem;
        font-weight: 800;
    }

    .service-modal-header p {
        margin: 0;
        color: #64748b;
        font-size: 0.66rem;
    }

    .service-modal-body {
        padding: 14px 16px;
    }

    .service-schedule-box {
        padding: 10px;
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 9px;
    }

    .service-active-switch {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        padding: 9px 10px;
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 9px;
    }

    .service-active-switch strong,
    .service-active-switch span {
        display: block;
    }

    .service-active-switch strong {
        color: #0f172a;
        font-size: 0.68rem;
    }

    .service-active-switch span {
        margin-top: 1px;
        color: #64748b;
        font-size: 0.58rem;
    }

    .service-modal-footer {
        display: flex;
        justify-content: flex-end;
        gap: 7px;
        padding: 10px 16px 14px;
        border-top: 1px solid #eef2f7;
    }

    @@media (max-width: 768px) {
        .service-list {
            max-height: none;
        }

        .service-row {
            align-items: flex-start;
            flex-direction: column;
        }

        .service-row-side {
            width: 100%;
            justify-content: space-between;
        }
    }

    @@media (max-width: 576px) {
        .service-summary-grid {
            grid-template-columns: 1fr;
        }

        .service-modal-card {
            max-width: 96vw;
        }

        .service-modal-footer {
            display: grid;
            grid-template-columns: 1fr;
        }
    }
</style>
